import 'package:flutter/material.dart';

class ArchivedWidg extends StatelessWidget {
  const ArchivedWidg({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('Archived Tasks'),);
  }
}
