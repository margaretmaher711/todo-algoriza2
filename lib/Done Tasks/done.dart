import 'package:flutter/material.dart';

class DoneWidg extends StatelessWidget {
  const DoneWidg({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('Done Tasks'),);
  }
}
